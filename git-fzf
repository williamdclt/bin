#!/usr/bin/env bash

set -e

commit=$( \
    git list-commits $@ | \
    fzf --ansi --multi \
        --preview 'git show --color {+1}' \
        --preview-window 'right:50%:hidden' \
)

echo -n "$commit" | awk '{printf $1}'

